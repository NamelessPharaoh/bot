__pycache__
db
db.bak
log
bot_token.txt
version
.python-version
*venv*
backups
captcha_images
.vscode
CLAUDE.md
ace-rollback-*
# Downloaded into the CWD by main.py's dependency setup whenever the suite
# imports main (tests/test_main_bootstrap.py does). The tracked list that
# actually ships lives at Bot-runtime/requirements.txt.
/requirements.txt
